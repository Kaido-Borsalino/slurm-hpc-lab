global:
  scrape_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['localhost:9093']

rule_files:
  - alert_rules.yml

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['c1:9100', 'c2:9100']
